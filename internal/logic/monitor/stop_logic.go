// Code generated by scorix.
package monitor

import (
	"context"
	"fmt"

	"github.com/tradalab/rdms/internal/svc"
	"github.com/tradalab/rdms/internal/types"
)

type StopLogic struct {
	ctx    context.Context
	svcCtx *svc.ServiceContext
}

func NewStopLogic(ctx context.Context, svcCtx *svc.ServiceContext) *StopLogic {
	return &StopLogic{
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

func (l *StopLogic) Stop(params *types.MonitorReq) (*types.Empty, error) {
	c, err := l.svcCtx.RedisManager.Get(params.ConnectionId, int(params.DatabaseIndex))
	if err != nil {
		return nil, err
	}

	c.MonitorMu.Lock()
	cancel := c.MonitorCancel
	c.MonitorMu.Unlock()

	if cancel != nil {
		cancel()
	}

	statusEvent := fmt.Sprintf("monitor:status:%s", params.ConnectionId)
	l.svcCtx.App.Evt().Emit(context.Background(), "", statusEvent, false)

	return &types.Empty{}, nil
}
