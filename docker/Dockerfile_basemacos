# 1. Base Linux image for cross-compilation
FROM ubuntu:22.04 AS base

ENV DEBIAN_FRONTEND=noninteractive
ENV GO_VERSION=1.24.10
ENV OSXCROSS_VERSION=v1.0
ENV SDK_VERSION=14.5

# Install dependencies
RUN apt-get update && apt-get install -y \
    ca-certificates \
    curl \
    wget \
    git \
    build-essential \
    pkg-config \
    cmake \
    patch \
    libxml2-dev \
    libssl-dev \
    zlib1g-dev \
    liblzma-dev \
    libbz2-dev \
    libz-dev \
    bash \
    python3 \
    python3-pip \
    xz-utils \
    cpio \
    && rm -rf /var/lib/apt/lists/*

# Install dmgbuild
RUN pip3 install dmgbuild

# Install Go
RUN wget -q https://go.dev/dl/go${GO_VERSION}.linux-amd64.tar.gz && \
    tar -C /usr/local -xzf go${GO_VERSION}.linux-amd64.tar.gz && \
    rm go${GO_VERSION}.linux-amd64.tar.gz

ENV PATH="/usr/local/go/bin:${PATH}"

# Install osxcross
# Note: This requires the macOS SDK to be present in the build context at tarballs/MacOSX${SDK_VERSION}.sdk.tar.xz
WORKDIR /osxcross
RUN git clone https://github.com/tpoechtrager/osxcross.git .

# We expect the SDK to be provided in the 'tarballs' directory
# If not present, the build will fail here during the actual build process
# COPY tarballs/MacOSX${SDK_VERSION}.sdk.tar.xz /osxcross/tarballs/

# Build osxcross
# RUN UNATTENDED=1 ./build.sh

ENV PATH="/osxcross/target/bin:${PATH}"

# Verification
RUN go version
